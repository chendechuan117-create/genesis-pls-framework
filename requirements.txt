# ── Core (required) ──
discord.py>=2.3.2
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.1
pysocks>=1.7.1
pydantic>=2.0.0
httpx>=0.27.0
numpy>=1.26.0

# ── API server ──
fastapi>=0.110.0
uvicorn>=0.27.0

# ── Embedding & Reranking (optional but recommended) ──
sentence-transformers>=2.5.0

# ── Web content extraction (optional, used by ReadUrlTool) ──
trafilatura>=1.8.0

# ── Browser automation (optional, used by ReadUrlTool fallback) ──
playwright>=1.42.0

# ── System monitoring (optional, used by JobManager) ──
psutil>=5.9.0

# ── Testing (required by doctor.sh test / test-diff) ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
